apiVersion: v1
kind: Service
metadata:
  name: kuberag-agent-gemini
  namespace: {{ .Release.Name }}
spec:
  selector:
    app: kuberag-agent-gemini
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
