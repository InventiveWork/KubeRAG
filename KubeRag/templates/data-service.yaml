apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-pipeline-service
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: kuberag-pipeline
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    app.kubernetes.io/name: kuberag-pipeline
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
  - protocol: TCP
    port: {{ .Values.service.pipeline.port }}
    targetPort: {{ .Values.service.pipeline.targetPort }}
    {{- if and (eq .Values.service.type "NodePort") (.Values.service.pipeline.nodePort) }}
    nodePort: {{ .Values.service.pipeline.nodePort }}
    {{- end }}
  type: {{ .Values.service.type }}