apiVersion: apps/v1
kind: Deployment
metadata:
  name: kuberag-agent-semantic-kernel
  namespace: {{ .Release.Name }}
spec:
  replicas: {{ .Values.frameworks.semantic-kernel.replicaCount }}
  selector:
    matchLabels:
      app: kuberag-agent-semantic-kernel
  template:
    metadata:
      labels:
        app: kuberag-agent-semantic-kernel
    spec:
      volumes:
        - name: secret-volume
          secret:
            secretName: {{ .Release.Name }}-secret
            defaultMode: 384
      containers:
      - name: kuberag
        image: {{ .Values.AGENT_IMAGE }}
        env:
        - name: LLM_FRAMEWORK
          value: "semantic-kernel"
        volumeMounts:
          - name: secret-volume
            mountPath: "/etc/secret-volume"
        envFrom:
        - configMapRef:
            name: {{ .Release.Name }}-cm
        - secretRef:
            name: {{ .Release.Name }}-secret
        ports:
        - containerPort: 5000
