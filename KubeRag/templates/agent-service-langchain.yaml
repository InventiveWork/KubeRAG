apiVersion: v1
kind: Service
metadata:
  name: kuberag-agent-langchain
  namespace: {{ .Release.Name }}
spec:
  selector:
    app: kuberag-agent-langchain
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
